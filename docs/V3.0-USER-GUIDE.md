# Javis-DB-Agent V3.0.0 功能使用手册

> 版本：V3.0.0 | 更新日期：2026-04-03

---

## 一、快速入门

### 1.1 首次登录

1. 访问 Web Dashboard (默认：http://localhost:8000)
2. 使用管理员账号登录
3. 完成初始配置

### 1.2 核心概念

| 概念 | 说明 |
|------|------|
| 会话(Session) | 一次完整的对话交互 |
| 工单(Ticket) | 需要审批的高风险操作 |
| 工具(Tool) | Agent可调用的操作能力 |
| Hook | 生命周期注入点 |

---

## 二、告警诊断

### 2.1 发起诊断

```
POST /api/v1/diagnose
{
  "alert_id": "ALT-2024-001",
  "instance_id": "INS-001",
  "alert_type": "cpu_high",
  "metrics": {...}
}
```

### 2.2 查看诊断结果

诊断结果包含：
- 根因分析
- 影响范围
- 建议操作

---

## 三、SQL分析

### 3.1 慢SQL诊断

```
POST /api/v1/analyze/sql
{
  "sql": "SELECT * FROM orders WHERE...",
  "database": "shop"
}
```

### 3.2 分析结果

| 项目 | 说明 |
|------|------|
| 执行计划 | EXPLAIN输出 |
| 耗时分析 | 各阶段耗时 |
| 优化建议 | 索引建议、重写建议 |

---

## 四、安全审批

### 4.1 自动审批

- L1-L3风险：自动执行
- L4风险：进入审批队列
- L5风险：自动阻断

### 4.2 人工审批

审批管理员收到通知后：
1. 查看操作详情
2. 评估风险
3. 批准/拒绝

### 4.3 Webhook审批

配置Webhook URL后，审批结果实时推送：
```python
# HMAC签名验证
import hmac, hashlib

def verify_signature(payload, signature, secret):
    expected = hmac.new(secret, payload, hashlib.sha256).hexdigest()
    return hmac.compare_digest(f"sha256={expected}", signature)
```

---

## 五、知识库

### 5.1 SOP查询

```
POST /api/v1/knowledge/sop
{
  "situation": "数据库连接超时"
}
```

### 5.2 案例检索

根据历史案例推荐解决方案。

---

## 六、Webhook配置

### 6.1 配置步骤

1. 进入「设置」→「Webhook」
2. 配置回调URL
3. 设置签名密钥
4. 配置IP白名单

### 6.2 回调事件

| 事件 | 说明 |
|------|------|
| `approval.completed` | 审批完成 |
| `approval.timeout` | 审批超时 |
| `session.expired` | 会话过期 |

---

## 七、API使用

### 7.1 认证

```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8000/api/v1/chat
```

### 7.2 主要API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/chat` | POST | 发起对话 |
| `/api/v1/diagnose` | POST | 告警诊断 |
| `/api/v1/analyze/sql` | POST | SQL分析 |
| `/api/v1/inspect` | POST | 数据库巡检 |
| `/api/v1/report` | POST | 生成报告 |
| `/api/v1/approvals` | GET | 查询审批 |
| `/api/v1/approvals/{id}/approve` | POST | 批准 |
| `/api/v1/approvals/{id}/reject` | POST | 拒绝 |

### 7.3 超时设置

| 端点 | 超时 |
|------|------|
| `/api/v1/chat` | 60秒 |
| `/api/v1/diagnose` | 30秒 |
| 其他 | 30秒 |

---

## 八、配置管理

### 8.1 配置文件

主要配置：`configs/config.yaml`

```yaml
ollama:
  base_url: http://localhost:11434
  model: qwen3.5:35b  # 推荐模型
  timeout: 120

api:
  host: 0.0.0.0
  port: 8000
```

### 8.2 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `OLLAMA_BASE_URL` | Ollama地址 | http://localhost:11434 |
| `OLLAMA_MODEL` | 模型名称 | qwen3.5:35b |
| `DB_PATH` | 数据库路径 | data/javis_db_agent.db |

---

## 九、日志与监控

### 9.1 日志位置

- 应用日志：`logs/javis-agent.log`
- 审计日志：`logs/audit.log`

### 9.2 指标监控

Prometheus格式指标：
```
# HELP javis_requests_total Total requests
# TYPE javis_requests_total counter
javis_requests_total{endpoint="/api/v1/chat"} 1234
```

---

## 十、故障排查

### 10.1 服务无法启动

1. 检查端口占用：`lsof -i:8000`
2. 检查配置：`./javis-agent --validate`
3. 查看日志

### 10.2 LLM调用失败

1. 检查Ollama状态：`curl http://localhost:11434/api/version`
2. 检查模型是否安装：`ollama list`
3. 检查网络连接

### 10.3 数据库连接失败

1. 检查PostgreSQL服务
2. 验证账号密码
3. 检查防火墙

---

*文档版本：V3.0.0 | 更新日期：2026-04-03*
